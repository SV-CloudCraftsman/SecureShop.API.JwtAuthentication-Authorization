# SecureShop.API.JwtAuthentication-Authorization
A production-style ASP.NET Core Web API implementing JWT authentication and authorization with secure token validation. Includes Swagger UI with JWT support and configurable settings via appsettings or environment variables (Azure App Service ready).
